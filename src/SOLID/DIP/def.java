package SOLID.DIP;

public class def {

    /*
     * High level modules should not depend on low level modules.Both should depend on abstractions
Abstractions should not depend depend on details.Details sholud depend on abstractions

The interaction between high level and low level modules should be thought of as an abstract interaction between them
    */
    
}
